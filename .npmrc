# npm 10 crashes resolving vitest 4.x peer graph (Cannot read properties of null (reading 'edgesOut')).
# Keep peer resolution in legacy mode so `npm install` / `npm ci` work locally and in CI.
legacy-peer-deps=true
